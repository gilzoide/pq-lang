#!/usr/bin/env pq

; Bom dia =]
(import rand :as rd)
(let a -0xA1 )
(let null ()) ; oie
(let bin 0b011)
(let oct 0o341)
(let float -1.1E-52f)
(let string "oie, como estão?")
(let b '(1 2))
(let h b.head)
(print (+ a b) (- a b))
