#!/usr/bin/env pq

; Bom dia =]
(import rand :as rd)
( let a -0xA1 )
(let null ( )) ; oie
(let bin 0b011)
(let oct 0o341)
(let float -1.1E-22f)
(let array [1 2 3 4])
(let string
  "oie, como estão vocês? Eu to bão, só testando umas string e pá")
(let b '(1 2))
(let h b.1 head)
(print (+ a b) (- a b))
